{
  "version": 2,
  "builds": [
    {
      "src": "api/stripe-webhook.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/webhook/stripe",
      "dest": "/api/stripe-webhook"
    }
  ]
}